# Scheduler uses firebase-admin credentials (same service account as backend)
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your-service-account-client-email@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=your_firebase_db_id

# Optional cron schedule (when running under a scheduler/provider)
CRON_SCHEDULE=@hourly  # example: '@daily', '0 3 * * *'